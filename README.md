# PDF to Word Converter
This is a Python script that converts PDF files to Word documents using Tesseract OCR and python-docx.

## How to use
- Install requirements: `pip install pytesseract python-docx`
- Run the script: `python convert.py`
